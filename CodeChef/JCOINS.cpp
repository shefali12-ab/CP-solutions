#include <iostream>
#include <bits/stdc++>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int X, Y;
        cin >> X >> Y;
        cout << (X * 10 + Y * 5) << endl;
    }
    return 0;
}