#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int tc;
    cin >> tc;
    while (tc--)
    {
        int D, C;
        cin >> D >> C;
        int A[3], B[3];
        for (int i = 0; i < 6; i++)
        {
            if (i < 3)
            {
                cin >> A[i];
            }
            else
            {
                cin >> B[i];
            }
        }
    }

    return 0;
}