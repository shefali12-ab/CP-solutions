/*
Given an array of integers and a number k,find the maximum sum of k consecutive elements.

*/
#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int>v;
    int n,k;//size of vector, number of consecutive element
    cin>>n>>k;
    while(n--)
    {
        int x;
        cin>>x;
        v.push_back(x);
    }
    while

    
}