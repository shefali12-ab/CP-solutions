// Maximum subarray sum
// if all the elements are positive then the maximum subarray sum is sum of all the elements of array
// if all the elements are negative then maximum subarry sum is the largest element
#include <iostream>
using namespace std;
int main()
{

    return 0;
}